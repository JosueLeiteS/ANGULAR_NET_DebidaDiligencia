import { pageOffshore, pageSanction, pageWorldbank } from "./pageModel";

export interface responseModel {

    pagOffshore: pageOffshore;
    pagSanction: pageSanction;
    pagWorldbank: pageWorldbank;

}